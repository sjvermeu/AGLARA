# Class: myportage::params
#
# Sets internal variables and defaults for portage configuration
class myportage::params {

  # Does the system use stable keywords (true) or ~arch (false)?
  $stable = 'true'
}
